source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in github_clawgrabber.gemspec
gemspec

group :test do
  gem 'bundler-audit'
  gem 'minitest', '~> 5.0'
  gem 'rake', '~> 10.0'
  gem 'ruby_audit'
  gem 'webmock', '~> 3.3.0'
end
